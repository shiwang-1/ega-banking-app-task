export const EMAIL_REGEX = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/;

export const PASSWORD_REGEX = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,16}$/;

export const NAME_REGEX = /^[^\s\d\W]+$/;

export const MAX_FIRST_NAME = 50;
export const MAX_lAST_NAME = 50;

export const MIN_FIRST_NAME = 2;
export const MIN_LAST_NAME = 2;

export const MIN_PASSWORD = 8;
export const MAX_PASSWORD = 16;
